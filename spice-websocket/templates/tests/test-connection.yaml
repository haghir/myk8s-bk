apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "spice-websocket.fullname" . }}-test-connection"
  labels:
{{ include "spice-websocket.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "spice-websocket.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
